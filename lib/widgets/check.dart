// classe (Lista) dei controlli
class Check {
  final String id;
  final String title;
  final bool checked;
  final String? description;

  const Check(this.id, this.title, this.checked, [this.description]);
}